{
  "name": "night",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ui && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
